# Crypto-Summary 環境変数設定例
# このファイルを .env にコピーして実際のキーを入力してください。
# .env は .gitignore に含まれているためリポジトリには含まれません。

# =====================================================================
# 【マルチユーザー / Docker】起動時に必須なのは以下の 2 つだけ
# =====================================================================

# セッション署名キー。未設定なら DATA_DIR に _session_key を自動生成して使う
# （0600 で保存され、再起動してもログインは切れない）。複数の環境で同じ
# セッションを共有したい場合だけ明示する:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# ユーザーDBを格納するディレクトリ（Docker volume のマウント先）
# これを設定するとマルチユーザーモード（Google ログイン）になる。
DATA_DIR=/data

# アプリの公開 URL。全エントリが https のときセッション Cookie に Secure が付く。
# 下の「初回セットアップウィザード」からも設定できる（BASE_URL の項）。

# =====================================================================
# 以下はすべて「初回セットアップウィザード」（ログイン前に表示）から
# Web 画面で設定できます。.env に書いておけばウィザードを省略できます。
# どちらでも可。env に書いた値が常に優先されます。
# =====================================================================

# --- Google OAuth 2.0（マルチユーザーでログインに必須）---
# Google Cloud Console → APIとサービス → 認証情報 → OAuth 2.0 クライアントID で取得
# リダイレクトURI には BASE_URL に挙げた各入口の /auth/callback を登録すること
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# アプリの公開URL（末尾スラッシュなし）。OAuth リダイレクトURI の組み立てに使う。
#
# カンマ区切りで複数書ける。ポート転送などで入口が 2 つあるときは両方を
# 並べておくと、開いた入口に応じたリダイレクト URI が使われる
# （Google 側にもそれぞれの /auth/callback を登録しておくこと）:
#   BASE_URL=https://example.com/crypto,http://localhost:8000
#
# サブパスで公開する場合（https://example.com/crypto/ のように階層を持たせる）
# は URL にパスまで書く。接頭辞は先頭エントリのパスから決まる（CS_ROOT_PATH で
# 明示もできる。空は自動、"/" はルート配信の強制）。リバースプロキシ側は
# 接頭辞を落とさずそのまま転送する設定にすること（nginx なら proxy_pass の
# 末尾にスラッシュを付けない）。詳細は docs/deploy.md。
BASE_URL=
#CS_ROOT_PATH=/crypto

# 暗号化マスター鍵（Web/CLI から登録する取引所・ウォレットAPIキーの暗号化に使用）
# 生成: crypto-summary account gen-key（ウィザードの「生成する」ボタンでも可）
# 紛失すると登録済みキーを復号できなくなる。
CS_SECRET_KEY=

# 管理者の Google アカウント（カンマ区切り・マルチユーザーモードのみ）
# ここに記載したメールのユーザーだけが、Web の「システム設定」から
# 全ユーザー共通のスキャン用 API キー（Etherscan / Helius）を編集できる。
ADMIN_EMAILS=

# =====================================================================
# Asset Summary 連携（任意）
# 兄弟アプリ Asset Summary からのサーバー間「読み取り専用」アクセスを許可する
# 共有トークン。未設定なら機能自体が無効（従来どおり）。
# Asset Summary 側の CS_SERVICE_TOKEN と同じ値を設定すること。
# 生成: python -c "import secrets; print(secrets.token_hex(32))"
# 使い方・応答形: docs/api.md
# =====================================================================
CS_SERVICE_TOKEN=

# =====================================================================
# CoinGecko Demo API キー（任意・無料）
# https://www.coingecko.com/en/api/pricing で「Demo」プランに登録 → API キー発行。
# 設定するとレート制限が 30 req/分 に緩和され、価格・推移グラフの取得が安定・高速化する。
# 未設定でもキーなしで動作するが、429（レート制限）が出やすくなる。読み取り専用。
# 注: 無効なキーを設定すると 401 で全価格取得が失敗する。発行したキーのみ入力すること
#     （空欄のままなら「キーなし」で動作する）。管理者設定の Web 画面からも登録できる。
# =====================================================================
COINGECKO_API_KEY=


# =====================================================================
# PBR Lending クローラー連携（任意）
#
# PBR Lending は当年分の公式取引履歴 CSV を出さないため、当年のデータは
# ローカルのクローラー（PBRLending-History-Check）で取得する。
# 出力ディレクトリを指すと、以下が有効になる:
#   - 画面を開いたときに新しいクロール結果を自動で取り込む
#   - インポート画面に「PBR Lending クローラー同期」カードが出る
#   - サイドバーに「PBR クローラー」タブ（ビューア画面）が出る
#   - CLI の crypto-summary sync-pbr で --crawl-dir を省略できる
# =====================================================================

# クローラーの outputs ディレクトリ
#   Windows 例: PBR_CRAWL_DIR=J:/Git/PBRLending-History-Check/outputs
#   Docker 例 : PBR_CRAWL_DIR=/pbr-outputs （docker-compose.yml のマウント先）
PBR_CRAWL_DIR=

# クローラーのリポジトリの場所。同一マシンでまとめて起動する場合
# （docker-compose.yml の include、および syncthing サービス）が参照する。
# Windows でも / 区切りで書く。
#   例: PBR_REPO_DIR=J:/Git/PBRLending-History-Check
PBR_REPO_DIR=

# クローラーのビューア画面 URL。書くとサイドバーに「PBR クローラー」タブが出る。
# 未設定ならタブを出さない（クラウド運用では不要。クロールは手元で行い、
# 結果はファイル同期で届く）。
# URL を開くのは利用者のブラウザなので、本体が Docker 内でもホストの
# 127.0.0.1 を指定してよい。クローラーをホストで動かすなら 4173、
# Docker で動かすなら 4174。
PBR_VIEWER_URL=

# --- ファイル同期の相手登録（任意・Syncthing を使う場合） ---
# 設定すると、インポート画面から同期相手のデバイス ID を登録できる（管理者のみ）。
# 手順は docs/syncthing-pairing.md を参照。
# API キーは Syncthing 全体（全フォルダ・全デバイス）を操作できるので、
# その Syncthing を持つ側にだけ渡すこと。
#   コンテナで動かす場合（推奨）: http://syncthing:8384
#   ホストで直接動かす場合      : http://127.0.0.1:8384
# 注: Docker のアプリからホストの Syncthing を見ることは基本できない。
#     GUI が 127.0.0.1 待受だと host.docker.internal 経由の要求は API キーを
#     付けても 403（ホストヘッダ検査）。Syncthing 側で insecureSkipHostcheck を
#     有効にしない限り通らない。
SYNCTHING_URL=
SYNCTHING_API_KEY=

# 同期フォルダのパスを、Syncthing コンテナから見た形で上書きする（任意）。
# 通常は不要で、未設定なら PBR_CRAWL_DIR がそのまま使われる。
# アプリと Syncthing でマウント位置が違う場合だけ設定する。
#   例: アプリからは /data/pbr-outputs、Syncthing からは /pbr-outputs
#       → SYNCTHING_FOLDER_PATH=/pbr-outputs
# 誤ったパスを渡すと、Syncthing はそこを黙って作ってしまい、接続できていても
# 1 件も転送されない状態になる（画面が「0 件」の警告を出す）。
SYNCTHING_FOLDER_PATH=


# =====================================================================
# 取引所・ウォレットの API キー（以下はすべて任意）
#
# 推奨: 取引所キー / ウォレットのスキャン用キーは、Web の「インポート」画面
#       から登録してください（ユーザーごとに暗号化して保存されます）。
#       その場合、下記の .env への記載は不要です（CS_SECRET_KEY のみ必要）。
#
# 下記は CLI 利用時、またはシングルユーザーで全体共通キーを使う場合の
# フォールバックです。
# =====================================================================

# --- bitFlyer API ---
# 権限: 「資産残高を見る」「取引履歴を見る」のみ付与すること
# 出金・送付・注文の権限は絶対に付与しないこと（セキュリティ要件）
BITFLYER_API_KEY=your_api_key_here
BITFLYER_API_SECRET=your_api_secret_here

# --- Etherscan V2 API（EVMマルチチェーン: Ethereum/Arbitrum/Polygon/Base/Optimism）---
# https://etherscan.io/myapikey で無料発行。読み取り専用（出金権限は存在しない）。
# 1つのキーで全対応チェーンを取得できる。全ユーザー共通のシステムキー。
# Web の「システム設定」（管理者のみ）から登録すると、この env より優先される。
# 注: 無料プランは Base / Optimism 非対応（有料プランが必要）。
ETHERSCAN_API_KEY=your_etherscan_api_key_here

# --- Helius API（Solana ウォレット）---
# https://dev.helius.xyz で無料発行。読み取り専用（送金権限は不要）。
# 1つのキーで Solana メインネットの取引履歴を取得できる。全ユーザー共通のシステムキー。
# Web の「システム設定」（管理者のみ）から登録すると、この env より優先される。
# （Solscan 無料プランは取引履歴 API が使えないため Helius を採用）
HELIUS_API_KEY=your_helius_api_key_here
