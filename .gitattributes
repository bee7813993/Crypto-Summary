# このリポジトリのテキストファイルは LF に統一する。
# Windows のエディタやツールが CRLF で書き戻しても、コミット時に LF へ
# 正規化されるため、改行だけで全行差分になる事故を防ぐ。
* text=auto eol=lf

# 取引所からエクスポートした CSV サンプルは実物のバイト列
# （CRLF）のまま保持する。パーサのテスト資材として原本性が重要。
*.csv -text
